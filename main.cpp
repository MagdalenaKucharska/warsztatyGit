#include <iostream>
#include <fstream>

using namespace std;

int main() {

	string line;
	signed long long suma = 0;
	ifstream myfile ("in1.txt");
	
	if (myfile.is_open()){
		
		while (getline (myfile, line)){
			
			if (line[0] = '+'){
				
			}
			else if (line[1] ==
		}
	}
}